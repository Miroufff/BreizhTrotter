<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        {% block stylesheets %}
<<<<<<< HEAD
            <link href=" {{ asset('./../css/master.css') }}" rel="stylesheet" />
=======
            <link href=" {{ asset('./../css/master.css') }}" rel="stylesheet" /> 
>>>>>>> e7bbe3cfe55db1a1f2cdef096e72836c2b5053c4
        {% endblock %}
    </head>
    <body>
        <div id='form-connection' class= "form row">
            <div class="container">
                <div class="row">
                    {% for type, messages in app.session.flashBag.all %}
                        {% for message in messages %}
                            <div class="{{ type }}">
                                {{ message|trans({}, 'FOSUserBundle') }}
                            </div>
                        {% endfor %}
                    {% endfor %}
                </div>

                <div class="col-sm-6 col-md-4 col-md-offset-4">
				    <h2 class="text-center login-title">Connectez-vous</h2>
				</div>


				<div class="account-wall">
					<form class="form-signin" action="{{ path("fos_user_security_check") }}" method="post">
					    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />

					    <label for="username">{{ 'Username'|trans }}</label>
					    <input class="form-control" type="text" id="username" name="_username" value="{{ last_username }}" required="required" />

					    <label for="password">{{ 'Password'|trans }}</label>
					    <input type="password" id="password" class="form-control" name="_password" required="required" />

					    <input type="checkbox" id="remember_me" class="form-control" name="_remember_me" value="on" />
					    <label class="checkbox pull-left" for="remember_me">{{ 'Remember me'|trans }}</label>

					    <input class="btn btn-lg btn-primary btn-block" type="submit" id="_submit" name="_submit" value="{{ 'Validez'|trans }}" />
					</form>
				</div>
            </div>
        </div>
    </body>
</html>

{% trans_default_domain 'FOSUserBundle' %}

