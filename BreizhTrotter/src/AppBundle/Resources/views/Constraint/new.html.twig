{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Constraint creation</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('activity_show', { 'id': activity.id }) }}">
                Back to the activity
            </a>
        </li>
    </ul>
{% endblock %}
