{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Edition de la contrainte</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('activity_show', { 'id': idActivity }) }}">
                Retour à l'activité
            </a>
        </li>
        {% if is_granted("ROLE_SCENARIO_DELETE") %}
            <li>{{ form(delete_form) }}</li>
        {% endif %}
    </ul>
{% endblock %}
