{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Edition du scenario</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('scenario_show', { id : entity.id }) }}">
            Retour au scenario
        </a>
    </li>
    {% if is_granted("ROLE_SCENARIO_DELETE") %}
        <li>{{ form(delete_form) }}</li>
    {% endif %}
</ul>
{% endblock %}
