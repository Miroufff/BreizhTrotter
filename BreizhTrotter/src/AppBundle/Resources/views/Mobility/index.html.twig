{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Mobility list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('mobility_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('mobility_show', { 'id': entity.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('mobility_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('mobility_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
