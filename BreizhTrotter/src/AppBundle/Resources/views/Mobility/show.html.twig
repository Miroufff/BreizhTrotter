{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Mobilité</h1>

    <table class="records_list table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="record_actions">
        <li>
            <a href="{{ path('mobility') }}">
                Retour à la liste
            </a>
        </li>
        <li>
            <a href="{{ path('mobility_edit', { 'id': entity.id }) }}">
                Modifier
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
