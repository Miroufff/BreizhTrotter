{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Deliverable</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Action</th>
                <td>{{ entity.action.id}}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ens_deliverable') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ens_deliverable_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
