{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Action edit</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('activity_show', { 'id': entity.activity.id }) }}">
                Back to the activity
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
