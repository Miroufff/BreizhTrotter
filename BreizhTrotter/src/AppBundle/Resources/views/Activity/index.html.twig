{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Activity list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Day</th>
                <th>Name</th>
                <th>Description</th>
                <th>Feasibility</th>
                <th>Idimage</th>
                <th>Idscenario</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ens_activity_show', { 'id': entity.id }) }}">{{ entity.day }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.feasibility }}</td>
                <td>{{ entity.idImage }}</td>
                <td>{{ entity.idScenario }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ens_activity_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ens_activity_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ens_activity_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
